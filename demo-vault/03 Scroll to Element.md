# Scroll to Element

Use `#id` to scroll the iframe to a specific element.

## Scroll to Section Gamma

```md
![[sections.html#gamma]]
```

<!-- obsidian-dev-utils-disable-next-line demo-vault-validation/no-wikilinks -- The note teaches Obsidian's embed syntax, so the live example is written the way a reader would write it. -->
![[sections.html#gamma]]

## Scroll to Section Alpha

```md
![[sections.html#alpha]]
```

<!-- obsidian-dev-utils-disable-next-line demo-vault-validation/no-wikilinks -- The note teaches Obsidian's embed syntax, so the live example is written the way a reader would write it. -->
![[sections.html#alpha]]

The iframe loads the full page but automatically scrolls to the targeted element.

> [!WARNING]
>
> The scrolling is this plugin's own work on the embedded document, so it stays in the note. The **Open in external browser** button ([11 Open in External Browser](<./11 Open in External Browser.md>)) hands the browser the plain file, which opens from the top.
